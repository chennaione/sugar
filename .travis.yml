language: android
jdk: oracle

env:
  global:
    - TERM=dumb
android:
  components:
    - sys-img-armeabi-v7a-android-23
    - tools
    - build-tools-23.0.2
    - android-23
    - extra-android-m2repository

script:
  - ./gradlew clean build connectedCheck coveralls --debug

cache:
  directories:
    - $HOME/.m2
